// 2553. Separate the Digits in an Array

#include <vector>

class Solution {
public:
     std::vector<int> separateDigits(std::vector<int>& nums)
     {

          std::vector<int> splited{};
          nums.reserve(nums.size() * 2); // assmuing every number is double digit


          for (auto& i : nums)
          {
               int temp{ i };
               


          }


     }
};